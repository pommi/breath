version: "3.7"
services:

  breath_dev:
    build:
      context: ./
    image: breath
    volumes:
        - ./breath.yml:/home/breath/app/breath.yml
    entrypoint: bash init-start.sh
    restart: on-failure
    cap_add: [ NET_ADMIN ]
